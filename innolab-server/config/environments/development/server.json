{
  "host": "${process.env.HOST || 'localhost'}",
  "port": "${process.env.PORT || 12220}",
  "proxy": {
    "enabled": true,
    "host": "${process.env.PROXY_HOST || 'localhost'}",
    "port": "${process.env.PROXY_PORT || 1337}"
  },
  "autoReload": {
    "enabled": true
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": true
  }
}
